<h1><%= @list.name%></h1>

<ul>
<% @list.movies.each do |movie| %>
  <li><%= movie.title %></li>
<% end %>
</ul>



<%= link_to "Add a new movie", new_list_bookmark_path(@list) %>
<br>
<%= link_to "Back", lists_path %>
